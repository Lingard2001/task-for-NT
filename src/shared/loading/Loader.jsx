import Icon from "../Icon"
import './Loader.css'
const Loader = () => {
    return (
        <div className="cart__loader">
                <Icon className={`uil uil-shopping-cart-alt`} size={`3rem`}/>
        </div>
    )
}

export default Loader